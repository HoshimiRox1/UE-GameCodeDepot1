# 代码风格

- 模块的脚本文件开头需要以中文注释说明模块功能
- 模块内部函数需要单行注释说明函数作用
- 代码风格保持简洁，如果能用50行就能实现功能，就不要写冗杂的100行
- 当用户让你修改或完善某个指定模块时，禁止修改其他模块。
- 如果经你判断，某个模块的修改和实现必须接触其他模块，需要向用户提供意见并征得同意
- 一个脚本文件需要的包一次性在脚本顶部import即可，不要局部import，会减少可读性
- 如果随着项目成长，有新的文件夹和文件生成，要在每步完成时及时更新gitignore避免新增的无关文件被git错误地追踪
- 每次完善新模块，都需要在项目文件结构内更新内容。如
  - 本次实现的模块内容和功能
  - 模块暴露的接口
  - 模块可供配置的参数项
  - 模块CLI调用的命令行参数项

# 错误修复

当用户告诉你项目运行遇到错误并给你日志的时候，不要直接去修改代码
而是按照以下格式执行操作：
- 提出1个问题：即该问题可能是怎么出现的
- 提出3个解决方案：怎么解决这个问题
- 提出1个建议

在用户确认让你修改代码之前，禁止修改代码

# 操作协同

当你需要执行经过用户同意的操作时，需要说明这个命令是为了做什么的。使得用户能把握你的执行进度和方向

# 开发执行

除非任务十分简单，否则一旦是具有规模的任务，你都需要自己先制定计划，然后一步步进行，而不是做一步看一步影响效率

# 关于虚幻

在该项目中，你所有的更改只能限制在两个文件夹内
- Source：虚幻的C++类代码目录
- BlueprintShowcase：以html展示的蓝图连接示意目录

除此以外的所欲文件夹你禁止操作

## Source - C++类的源码

不允许新增文件，因为虚幻的C++类是通过编辑器内部工具新增创建的。
你只能修改已有文件

## BlueprintShowcase - 蓝图连接示意流程图

这个文件是用于给用户展示如何连接蓝图各个节点的，你可以直接以一个流程图的形式描绘各个蓝图节点以及它们之间的连线
至于如何绘制，详见 visualize Skill 是如何说明的

为了避免因为蓝图过多导致的文件杂乱，BlueprintShowcase中只有一个BlueprintShowcase.html文件
顶部一个『导航栏』包含许多跳转方块，每个方块内部文本显示是那个蓝图类的。
每个蓝图内